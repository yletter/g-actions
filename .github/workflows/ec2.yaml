name: Deploy EC2 with Terraform

on:
  workflow_dispatch:  # manual trigger
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform   # all run steps will execute here

    permissions:
      id-token: write   # required for OIDC
      contents: read    # required to checkout repo

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::050451371849:role/GitHubActionsTerraformRole
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve

      - name: Wait for 5 minutes
        run: sleep 300   # 300 seconds = 5 minutes

      - name: Terraform Destroy
        run: terraform destroy -auto-approve

